# FoodRush Backend

Spring Boot 3.x API (Java) — MySQL, Spring Security + JWT, layered package structure (controller, service, repository, entity, dto, config, security, exception).

Scaffolding happens via Task 1 in `.taskmaster/tasks/tasks.json`.
