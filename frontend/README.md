# FoodRush Frontend

React 18 + Vite — react-router-dom, axios, Tailwind CSS. `src/components`, `src/pages`, `src/services`, `src/contexts`, `src/utils`, `src/hooks`.

Scaffolding happens via Task 1 in `.taskmaster/tasks/tasks.json`.
